# Lumy 型号提取与 RAG 问答——简要方案说明

> 任务：5 份电子元器件规格书（MAX20029 / LM358 / TL431 / D-CRCW / LM317）的型号提取（结构化）+ 双索引 RAG 问答。
> 最终结果：34 家族 / 66 PPN / 553 OPN / 8 命名规则入库；13 道自测问答六类全覆盖，通过率 13/13。

## 一、解题思路

把问题拆成"**结构化的归结构化，语义的归语义**"两条链路，用一套 LangGraph 编排：

```
导入：PDF ──┬─ 链路B（结构化）：pdfplumber 解析 → LLM 区域标注 → 四层型号提取 → PostgreSQL
           └─ 链路A（语义）：  带页码分块 → BGE-M3 稠密+稀疏向量 → Milvus
查询：问题 → 意图识别（六类）→ ┬ 结构化检索（PG：层级遍历/规则文法）
                              └ 向量+HyDE 检索（Milvus）→ RRF → Rerank → 证据引用式生成/拒答
```

核心判断：**型号清单、命名规则是"精确问题"**（枚举、逐位解码），向量检索天然不适合（同族型号字符串高度相似、易混）；**参数、比较是"语义问题"**（描述多样、证据在正文表格里），规则又覆盖不全。双索引各取所长，答案层用证据契约把两者缝合。

LLM 的定位是"边界点专项调用"（区域划界、文法解码、异常归属、答案生成），**枚举和层级永远由确定性代码+数据库完成**——保证型号字符串 100% 可溯源、可复现。

## 二、解析方式（pdfplumber，纯文本层，无 OCR）

5 份均为原生文本型 PDF，无需 OCR/多模态。pdfplumber 基于**矢量线坐标**提取表格，三个关键处理：

1. **合并单元格 = cell bbox 覆盖判定**。pdfplumber 对被合并覆盖的位置返回 None，但合并格自身的 bbox 会覆盖多个行/列条带。据此做精确的向下/向右填充——真正"空"的单元格（bbox 只占一个条带）不会被误填。反例教训：盲目双向填充会把 LM317 对比表的 `0 to 125` 灌进热阻列、把 dcrcw 的尺寸码灌进阻值行。
2. **跨页续表合并**：相邻页 + 列数一致 +（后表首行与前表头相似度 ≥0.5 → 去重复表头拼接 ‖ 或几何延续：前表底 ≥80% 页高且后表起于页顶 30%）。实测：tl431 EC 表跨 9 页合并 86 行、lm358 封装附录跨 8 页合并 196 行。
3. **页眉页脚剔除**：在 ≥50% 页面重复出现的行自动识别删除；每个文本块/表格天然携带页号——满足"证据带页码"的要求。

## 三、规则处理（Lumy 四层定义的落地）

**层级判定的唯一标准是编码位的属性性质**：功能参数 → PPN 位，订购属性（封装/包装/环保/温度档）→ OPN 位，**与字符在字符串中的位置无关**（Maxim 的 MAX20029ATIA/V+ 中封装码 A 夹在家族名与配置字母之间，PPN 不是 OPN 的连续前缀）。

提取按来源优先级，**只从结构化位置枚举，从不全文扫型号**：

| 优先级 | 来源 | 实例 |
|---|---|---|
| 1 | Selector Guide / EC 表 / 对比表逐条 | MAX20029=18 个、TL431/432=18 个档位组合 |
| 2 | 命名规则文法解码（**只解码不展开**） | LM317yyyz、MAX20029ATI_/V+、CRCW 阻值码 |
| 3 | 订购表 / 封装附录 Orderable part 列 | LM317=15、TL431=331、LM358=187（含 5962-* 军品号） |
| 4 | Features 缩写（仅参考，须拆开） | 不作为枚举依据 |

**四道反误提防线**：① 区域角色先行——同一字符串在订购表列里是封装属性、在正文里是被引器件；② 黑名单——封装名/图号/文档编号（SLVS044Z）/认证号（AEC-Q200）模式过滤；③ **源文锚定**——家族/PPN 必须在原文以独立词出现（拦截 LLM 幻觉，也拦截截断误报如把 TL431 截成 TL4）；④ 封面 token 交叉校验 + PPN/OPN 同串去重。

**展开纪律**：命名规则 `expanded=false`，绝不笛卡尔积。dcrcw 的 阻值×容差×E系列 只记录文法和文档列出的 2 个显式 OPN；MAX20029 的 A~H 配置只取 Selector Guide 实际列出的 18 个，不做 配置×封装×温度 全组合。

## 四、分块与检索策略

**分块（链路A）**：表格线性化（表头+行，>25 行切片且每片重复表头）+ 页文本按 ~700 字符累积成块；每块带 `page/page_end/file_title`（Milvus 动态字段，免改 schema）。共 496 块。

**双索引查询路由**（六类意图：型号清单/型号详情/规则解释/参数查询/跨文档比较/无答案）：

- **结构化检索先行**：严格对齐（OPN/PPN/家族精确匹配）→ 失败降级前缀对齐（用户输入常是前缀，"中间态下沉"列出下一级清单）。家族→PPN→OPN 层级遍历、命名规则 positions 逐位解释都走 PG。
- **结构化结果反哺向量检索**：命中型号所在文件作为 Milvus 的 `file_title` 标量过滤（比按 item_name 过滤更稳——多家族 PDF 的 item_name 是拼接串，`in` 精确匹配会漏）。
- **融合排序**：向量 + HyDE 两路 RRF（k=60，按 chunk_id 去重）→ BGE-Reranker 交叉编码精排（sigmoid 归一化）。结构化证据**不参与**重排竞争，作为事实直接进答案上下文。

**证据契约（写进 prompt 的硬约束）**：每条结论标注 `[文件名 第N页]`/`[型号库]`；参数必须带数值+单位+测试条件+typ/min/max 属性，原文没有的不编造；跨文档比较分别列来源、冲突明示；**双源皆空或向量 top 分数 < 0.4 时明确拒答并说明缺什么**——两道无答案题（价格、库存交期）均正确拒答。
