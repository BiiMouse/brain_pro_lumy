# shopkeer_brain 问题解决记录

> 持续更新的问题解决日志。每条按 **现象 → 根因 → 修复（含代码片段）→ 验证 → 教训** 结构记录。
> 新问题追加到末尾，保持正序。代码片段仅展示改动相关部分，完整代码以仓库为准。
> 维护者：人（主）+ Claude（辅助）。最后更新：2026-09-17。

---

## 目录

- [背景与方法论](#背景与方法论)
- [运行 eval 的两个坑（Windows）](#运行-eval-的两个坑windows)
- [问题 1：拒答能力缺失 + 答案无出处（prompt 改造 + 拒答闸门）](#问题-1拒答能力缺失--答案无出处prompt-改造--拒答闸门)
- [问题 2：缺少可量化的评测（eval_plus.py）](#问题-2缺少可量化的评测eval_pluspy)
- [问题 3：越界问题被 web 搜索兜底，导致漏拒](#问题-3越界问题被-web-搜索兜底导致漏拒)
- [问题 4：reranker 分数与拒答阈值量纲不一致](#问题-4reranker-分数与拒答阈值量纲不一致)
- [问题 5：可回答问题被 LLM 误拒（上下文预算截断）](#问题-5可回答问题被-llm-误拒上下文预算截断)
- [问题 6：RAGAS embeddings wrapper 被 dict 返回值打崩（AttributeError）](#问题-6ragas-embeddings-wrapper-被-dict-返回值打崩attributeerror)
- [问题 7：RAGAS 判分重试风暴把真错掐成 TimeoutError](#问题-7ragas-判分重试风暴把真错掐成-timeouterror)
- [问题 8：eval 共用 session_id，chat_history 污染检索导致负例漏拒](#问题-8eval-共用-session_idchat_history-污染检索导致负例漏拒)
- [问题 9：is_refusal 散词匹配把正常回答的"资料不足部分"误判成拒答](#问题-9is_refusal-散词匹配把正常回答的资料不足部分误判成拒答)
- [问题 10：云 GPU 环境搭建四坑（镜像挂起 / HF 拉不动 / ModelScope 嵌套路径 / transformers API 变更）](#问题-10云-gpu-环境搭建四坑镜像挂起--hf-拉不动--modelscope-嵌套路径--transformers-api-变更)
- [问题 11：手写 SFT 数据的格式陷阱（句级编号 / 分隔符 / 考卷污染 / GBK 编码）](#问题-11手写-sft-数据的格式陷阱句级编号--分隔符--考卷污染--gbk-编码)
- [问题 12：照抄基准错位——PDF 原文 ≠ Milvus 里的 chunk 文本](#问题-12照抄基准错位pdf-原文--milvus-里的-chunk-文本)
- [问题 13：肉眼对不了空格——逐字校验必须机器做](#问题-13肉眼对不了空格逐字校验必须机器做)
- [问题 14：CSV→alpaca 转换——训练集如何对齐推理格式（含 neg 分数闸门）](#问题-14csvalpaca-转换训练集如何对齐推理格式含-neg-分数闸门)
- [问题 15：判据又撞模板文本——"无参考答案"四个字每条 instruction 里都有](#问题-15判据又撞模板文本无参考答案四个字每条-instruction-里都有)
- [问题 18：DPO 训练指标漂亮、生成质量倒退——过矫正与指标脱钩](#问题-18dpo-训练指标漂亮生成质量倒退过矫正与指标脱钩)
- [问题 19：Reranker 精排 78s 疑云——算力物理账与三重优化（线程引导 / max_length / 模型 A/B）](#问题-19reranker-精排-78s-疑云算力物理账与三重优化线程引导--max_length--模型-ab)
- [变更日志](#变更日志)

---

## 背景与方法论

- **项目**：`D:\AIpractise\shopkeer_brain\knowledge`，RAG 知识库助手。当前用 DeepSeek-v4 API；虚拟机 `192.168.245.128` 跑 Milvus / Neo4j / Mongo / MinIO；本地 BGE 做 embedding + reranker。
- **微调目标（五项能力）**：① 行业术语/格式 ② 检索内容忠实（防幻觉）③ 多片段整合 ④ 证据引用与出处 ⑤ 资料不足拒答。
- **策略**：prompt / 检索 / 编排 优先，微调兜底。②⑤ 主要靠检索+编排+prompt；微调真正值钱的是 ①。
- **控制变量三组对比**（衡量微调收益的唯一靠谱方法）：
  - **A** = DeepSeek-v4 + 好检索（强基线）
  - **B** = Qwen2.5-7B 原始 + 同一检索（**微调前必须抓存档**）
  - **C** = Qwen2.5-7B 微调 + 同一检索
  - 微调净收益 = C − B；B − A = 自部署质量代价。
  - **索引（chunking）是三组共用的控制变量，已冻结**，不再改动，免得反复重存档 Config A。

---

## 运行 eval 的两个坑（Windows）

1. **必须带 UTF-8 环境变量**，否则 `VectorSearchNode` 打印含 `•`（U+2022）的检索内容时 GBK 控制台崩（`'gbk' codec can't encode`）：
   ```bash
   PYTHONUTF8=1 PYTHONIOENCODING=utf-8
   ```
2. **必须在 `knowledge/` 目录下跑**：所有 `load_dotenv()` 都不带路径、按 CWD 找 `.env`，而 `.env` 只在 `knowledge/.env`。venv 在项目根 `.venv/Scripts/python.exe`。eval 直接 `query_app.invoke()`，**不用起 query_router 服务**；只需 VM + 本地 BGE + DeepSeek API。

完整命令：
```bash
cd knowledge && PYTHONUTF8=1 PYTHONIOENCODING=utf-8 ../.venv/Scripts/python.exe ../eval/eval_plus.py [--no-ragas]
```
- `--no-ragas`：只跑引用/#5拒答/阈值校准，不调 LLM 评判，快，适合调参。
- 不带：跑完整 RAGAS 5 指标，慢，用于存档基线。

---

## 问题 1：拒答能力缺失 + 答案无出处（prompt 改造 + 拒答闸门）

**现象**：早期模型对越界问题（天气/竞品/闲聊）强行编造回答（漏拒，违反能力⑤）；可回答的答案不带出处（违反能力④）。

**根因**：① prompt 没有强制"忠实+引用+拒答+格式"的硬约束；② 没有"资料不足就别调 LLM"的前置闸门，越界问题一路走到 LLM 被自由发挥。

**修复**：

(1) 重写答案 prompt（`knowledge/prompts/query/query_prompt.py` 的 `ANSWER_PROMPT`），用规则强制 ④⑤：
```python
ANSWER_PROMPT = """你是"掌柜智库"智能助手，必须严格依据【参考内容】回答用户问题。

【回答规则】
1. 忠实作答：仅依据【参考内容】回答，严禁编造任何未在参考内容中出现的事实、数字、参数、政策或结论；参考内容未提及的，宁可不说，不要凑数或猜测。
2. 出处标注：【参考内容】中每条记录都以 [编号] 标注（如 [0]、[1]）。你回答中每一条事实性结论，都必须在末尾标注来源编号……多条来源用 [0][2] 形式并列。
3. 资料不足：若【参考内容】为"无参考答案"，或与问题无关、无法支撑回答，请直接回复"资料不足，暂无法回答该问题。"并简要说明缺少哪类信息，不要展开臆测。
4. 结构清晰：优先"结论先行 + 分点说明"……
5. 表述差异：若多个片段表述不一致，请指出差异及各自的适用范围/条件，不要强行合并。
6. 图片：仅当问题确实需要图片辅助……才在答案最后追加一个独立的图片区块……

【参考内容】
{context}
……
请回答："""
```

(2) 在 `answer_node.py` 加**资料充足性闸门**——检索为空或最高相关性低于阈值就直接拒答，**不调用 LLM**（省钱+防幻觉）：
```python
# answer_node.py —— AnswerOutputNode.check_context_sufficient
def check_context_sufficient(self, state):
    """返回拒答文案(str)则拒答；返回 None 表示资料充足，继续走 LLM。"""
    docs = state.get("reranked_docs") or []
    threshold = get_config().rag_refuse_threshold   # 默认 0.4
    if not docs:
        return "资料不足，暂无法回答该问题。\n\n缺少的信息：知识库中未检索到与该问题相关的资料。……"
    top_score = docs[0].get("score") or 0.0
    if top_score < threshold:
        return (f"资料不足，暂无法可靠回答该问题（最高相关度 {top_score:.2f}，"
                f"低于阈值 {threshold}，为避免误导不作答）。\n\n……")
    return None
```

(3) 阈值配置（`config.py`）：
```python
rag_refuse_threshold: float = field(
    default_factory=lambda: float(os.getenv("RAG_REFUSE_THRESHOLD", "0.4"))
)
```

**验证**：见问题 3/4/5 的验证结果（拒答准确率最终 1.0、引用率 1.0）。

**教训**：拒答最好做在 LLM 之前（基于检索分数的硬闸门），既省钱又确定；prompt 里的"资料不足"规则是第二道防线，处理"检索到但实际无关"的情况。

---

## 问题 2：缺少可量化的评测（eval_plus.py）

**现象**：改 prompt / 改阈值都靠肉眼看几个例子，没有量化指标，无法判断"改好了没"、无法给 A/B/C 对比提供数字。

**根因**：缺一套覆盖微调五能力（尤其 ④引用、⑤拒答）+ 阈值校准的评测脚本。原 `eval.py` 只有 RAGAS 5 指标，没覆盖 ④⑤。

**修复**：新增 `eval/eval_plus.py`，复用 `eval.py` 的 RAGAS 通路，补三块：

```python
# 数据分工（互不污染）
# qa.csv     : 可回答用例 (question, ground_truth) → RAGAS 5 指标 + 引用指标
# qa_neg.csv : 该拒答用例 (question, note)         → 拒答准确率 + 分数分布

# #4 引用指标：统计答案中 [编号] 是否出现、编号是否落在检索文档范围内
def citation_metrics(answer, num_docs):
    cites = [int(c) for c in CITATION_RE.findall(answer)]   # \[(\d+)\]
    valid = [c for c in cites if 0 <= c < num_docs]
    return {"citation_count": len(cites), "has_citation": bool(cites),
            "citation_valid": len(valid) == len(cites),
            "citation_valid_rate": len(valid) / len(cites) if cites else 0.0}

# #5 拒答检测：匹配拒答文案特征（与 check_context_sufficient 模板一致）
REFUSAL_MARKERS = ("资料不足", "暂无法", "低于阈值")
def is_refusal(answer):
    return any(m in answer for m in REFUSAL_MARKERS) if answer else False

# 阈值校准：打印可回答集/拒答集的 reranker 最高分分布，用于定 RAG_REFUSE_THRESHOLD
# 若 可回答最低分 > 拒答最高分 → 完全可分，阈值取中点
```

**验证**：后续每次改动都用它量化（见问题 3/4/5）。`--no-ragas` 模式几秒出结果，适合迭代调参。

**教训**：先建评测再改代码。没有量化指标的"调优"是玄学。`--no-ragas`（不调 LLM 评判）和完整 RAGAS 两种模式分开用：前者快速调参，后者存档基线。

---

## 问题 3：越界问题被 web 搜索兜底，导致漏拒

**现象**：第一轮 eval 拒答准确率只有 **0.667**——"明天北京天气""iPhone 17 Pro 参数""你好"等越界问题没被拒，反而被 web 搜索结果兜底回答了。

**根因**：查询流程里有 web 搜索节点，越界问题触发 web 兜底 → web 片段相关性分数（改前 top_score 6.78/3.67 是 web 片段）过了拒答阈值 → 绕过了拒答。本项目是领域知识库助手，web 兜底反而是漏拒的源头。

**修复**：加开关默认关闭 web 搜索，建立纯 KB 基线。

(1) `config.py` 加开关：
```python
# 是否启用 Web 搜索兜底。默认关闭——越界问题被 web 兜底会绕过拒答阈值，导致漏拒。
enable_web_search: bool = field(
    default_factory=lambda: os.getenv("ENABLE_WEB_SEARCH", "false").lower() == "true"
)
```

(2) `web_search_node.py` 关闭时短路返回空，不调 MCP：
```python
async def _async_process(self, state):
    print(f"\n========== WebSearchNode 开始 ==========")
    # 资料充足性闸门：默认关闭 web 搜索。避免越界问题被 web 兜底绕过拒答阈值。
    if not self.config.enable_web_search:
        print(f"Web 搜索已关闭（enable_web_search=False），跳过 MCP 调用")
        self.logger.info("Web 搜索已关闭，跳过")
        return {"web_search_docs": []}
    ...
```

**验证**：拒答准确率 **0.667 → 1.0**。天气/iPhone/"你好"改前被 web 兜底误答，改后 top_score 0.0005/0.0003/0.0044，在闸门硬拒、不调 LLM。

**教训**：领域知识库的 web 兜底是双刃剑——能补 KB 不足，但也给越界问题开了后门。要"条件兜底"（KB 不足时才用 web）而不是无条件兜底。当前先关掉建纯 KB 基线，后续要做条件兜底再在此分支扩展。

---

## 问题 4：reranker 分数与拒答阈值量纲不一致

**现象**：拒答阈值设 0.4，但 reranker 实际返回的是原始 logit（范围约 **−6 ~ +8**），跟 0~1 的阈值完全不在一个量纲——阈值判断根本失真。

**根因**：`bge-reranker-large.compute_score()` 默认返回原始 logit；要 0~1 概率必须传 `normalize=True`（sigmoid）。原代码没传。

**修复**：

(1) `config.py` 加归一化开关（默认开）：
```python
# reranker 分数是否做 sigmoid 归一化（True→0~1 概率）。
# 必须为 True 才能与 RAG_REFUSE_THRESHOLD 量纲一致。
rerank_normalize: bool = field(
    default_factory=lambda: os.getenv("RERANK_NORMALIZE", "true").lower() == "true"
)
```

(2) `multi_search_rerank.py` 把开关传给 `compute_score`，并兜底单条输入返回标量的问题：
```python
normalize = self.config.rerank_normalize
reranker_score = reranker_model.compute_score(
    sentence_pairs=query_doc, normalize=normalize)

# 单条输入时 compute_score 返回标量，统一成列表便于 zip
if not isinstance(reranker_score, list):
    reranker_score = [reranker_score]
```

**验证**：分数归一化到 0~1。阈值校准**完全可分**：可回答最低分 ≫ 拒答最高分，中点约 0.376，**0.4 保留**。

**教训**：阈值和分数必须在同一量纲。接第三方模型/打分器时，先确认它返回的是什么（原始 logit？概率？相似度？），再定阈值，否则阈值是拍脑袋。

---

## 问题 5：可回答问题被 LLM 误拒（上下文预算截断）

**现象**：第二轮 eval 后，5 条可回答里有 **2 条被误拒**——Q2「卡纸」、Q3「清洁」。诡异的是，eval 结果的 `context` 列里**明明有答案**（Q2 有"纸张与定影单元…火灾"、Q3 有"…有机溶剂…无绒干抹布"，与 ground_truth 逐字一致），LLM 却说"并未明确提及"。

**根因**（关键诊断）：是 **LLM 喂入前的上下文预算截断**，不是召回/阈值/切片问题。

- eval 的 `context` 列存的是 `step_3_extract_context_from_reranked_docs` 返回的**检索全量**（按 rerank 分数排序），**不是** LLM 真正看到的。
- `answer_node.format_reranker_docs` 原本**硬编码 2500 字符全局预算**：
  ```python
  if used + len(data) > 2500:   # ← 硬编码，且 config 里的 max_context_chars(12000) 根本没接上
      break
  ```
- 以 Q2 算预算消耗（rerank 顺序）：

  | rerank 序 | chunk | 字符量 | 累计（含 meta） |
  |---|---|---|---|
  | 0 | 电源线（一大段 bullets） | ~1100 | ~1190 |
  | 1 | 产品安全手册 | ~850 | ~2130 |
  | 2 | 设备保修和责任 | ~350 | **2570 > 2500 → break** |
  | **4** | **设备（含「卡纸…火灾」答案）** | ~1100 | **永远进不了 LLM 窗口** |

  答案在 rerank 第 4 位（设备节），但 top-1 超长「电源线」+ top-2「产品安全手册」就吃满 2500，答案条目被截断挤出 LLM 可见窗口 → LLM 诚实地说"未提及"。Q3 同构（答案在设备节，也被截掉）。

**修复**：把硬编码预算换成 config 驱动的**总预算 + 单条截断**。

(1) `config.py` 新增单条上限（总预算复用已有的 `max_context_chars=12000`，它原本是死配置）：
```python
# 单条检索文档喂给 LLM 的最大字符数。超长 chunk 会被截断，
# 防止单条吃掉整个上下文预算、把含答案的后续条目挤出 LLM 可见窗口。
# 与 max_context_chars（总预算）配合：总预算控制窗口大小，单条上限控制公平分配。
rag_doc_max_chars: int = field(
    default_factory=lambda: int(os.getenv("RAG_DOC_MAX_CHARS", "2000"))
)
```

(2) `answer_node.format_reranker_docs` 重写预算逻辑——总预算 2500→12000，单条超限**截断**（不再 break 跳过）：
```python
def format_reranker_docs(self, reranked_docs):
    config = get_config()
    total_budget = config.max_context_chars      # 12000（原硬编码 2500）
    per_doc_cap = config.rag_doc_max_chars       # 2000，防大块独占

    result = []
    used = 0
    image_urls = []

    for index, doc in enumerate(reranked_docs):
        content = doc.get("content", "") or ""
        if not content:
            continue
        # 单条截断：超长 chunk 只取前 per_doc_cap 字符，避免一整节手册独占预算
        if len(content) > per_doc_cap:
            content = content[:per_doc_cap] + "\n…(该片段过长，已截断)"

        meta = {"score": doc.get("score", 0), "url": doc.get("url", ""), "title": doc.get("title", "")}
        data = f"[{index}]:{meta}\n{content}"

        if used + len(data) > total_budget:
            break
        result.append(data)
        used += len(data)

        image_urls.extend(self.extract_images_from_doc(doc))
    ...
```

**验证**（`--no-ragas`）：
- 误拒 **2/5 → 0/5**（摘要里 `⚠️ 误拒答` 行消失，5 条可回答全部作答）。
- Q2 答出"纸张与定影单元长时间接触→火灾"、Q3 答出"禁有机溶剂/喷雾，改用无绒干抹布"，均命中 ground_truth。
- #4 引用 1.0、#5 拒答 1.0、阈值仍完全可分（0.780 ≫ 0.010），**零回归**。
- `top_rerank_score` 分布与修复前一致（没动检索），确认因果只来自预算。

**教训**：
1. **诊断要分清"检索到了吗"vs"LLM 看到了吗"**。eval 的 context 列是检索全量，LLM 看到的是截断后产物——两者不同。误拒时先确认 LLM 实际输入，别一上来就怪召回或切片。
2. **死配置是隐患**：`max_context_chars` 早就在 config 里定义了，却没被 `format_reranker_docs` 用上，硬编码 2500 抢了位置。接配置时要确认它真的被消费了。
3. **单条截断 > 总预算 break**：纯抬高总预算也能解决，但加单条上限更鲁棒——防止某个巨型 chunk（大表格/整节手册）永远独占窗口。
4. **切片重排不是必需**：本案一度怀疑"切片太粗（整节当一个 chunk）"要重索引，但证据表明纯预算问题。索引因此冻结，省了一次重嵌入。

---

## 问题 6：RAGAS embeddings wrapper 被 dict 返回值打崩（AttributeError）

**现象**：`--smoke 2` 跑 RAGAS 直接 `AttributeError: 'dict' object has no attribute 'tolist'`，五指标一个都出不来。

**根因**：`BGEM3EmbeddingFunction.encode_documents()` 在当前 pymilvus 版本返回 **dict**（`{"dense": [...], "sparse": ...}`），不是带 `.dense` 属性的对象。原 eval.py 的 wrapper 用 `hasattr(result, 'dense')` 判断——对 dict **恒为 False**，于是掉进 `result.tolist()` 的兜底分支，对 dict 调 `.tolist()` 崩掉。典型的"接口版本变了，鸭子类型判断没跟上"。

**修复**：把 wrapper 用 `isinstance` 显式兼容两种返回形态（`eval_plus.py` 的 `LangchainCompatibleEmbeddings`）：

```python
def embed_documents(self, texts):
    result = self._bgem3_client.encode_documents(list(texts))   # 批量编码
    dense = result["dense"] if isinstance(result, dict) else result.dense
    return [d.tolist() for d in dense]
```

顺带把 RAGAS 通路（wrapper、上下文提取）从已弃用的 `eval.py` **内联进 `eval_plus.py`**，eval_plus 从此零依赖（eval.py / eval01.py 待删）。

**验证**：`--smoke 2` 绿，2 条 × 5 指标全出值。

**教训**：跨库包装第三方返回值时，`hasattr` 鸭子判断对"同一函数跨版本换返回类型"毫无防御——用 `isinstance` 显式列出已知形态，未知形态让它 loudly fail 也比错误兜底好。另外依赖已弃用模块的公共工具要趁早内联，弃用模块拖得越久迁移成本越高。

---

## 问题 7：RAGAS 判分重试风暴把真错掐成 TimeoutError

**现象**：RAGAS 判分偶发 `TimeoutError`（RAGAS 内部 `asyncio.wait_for` 的 180s 预算），且重试参数越调大越容易超时——8 次重试 × 30s 等待的配置几乎必超。

**根因**：**重试风暴**——`max_retries=8 × max_wait=30` 把 180s 预算吃光。真正的问题是混淆了两类失败：
- **快失败**（429 限流、JSON 解析错）：重试有意义，几百毫秒内出结果；
- **慢调用**（faithfulness / answer_correctness 的长结构化 prompt 单次近 200s）：**重试救不了**——两次慢调用本身就塞不进一个超时预算，越重试越超时，最后把真实错误掐成误导性的 TimeoutError。

**修复**（`eval_plus.py`，三层分工）：

```python
JUDGE_TIMEOUT = 300.0      # 单客户端超时 180→300：慢 prompt 留足一次调用的时间
RAGAS_MAX_RETRIES = 2      # 重试 8→2：只救快失败，不做无谓的慢调用重试
NAN_RETRY_ROUNDS = 3       # 按行补跑：判分后哪格 NaN 就整行重判，最多 3 轮——治慢调用
```

```python
# run_ragas_with_retry：raise_exceptions=False 让单格失败变 NaN 而非炸全场，
# 轮间只补含空缺的行，全绿或轮次用尽即停；残留空缺逐格打印（配合 --smoke 抓真实报错）
idxs = [gi for gi in idxs if any(scores[col][gi] is None for col in RAGAS_SCORE_COLS)]
```

**验证**：全量 RAGAS 5×5 全出值；偶发单格 Timeout（Job[20]）后第 1 轮补跑即补齐，摘要打印无空缺。

**教训**：**重试只救快失败，救不了慢调用**。看到"加大重试反而更多超时"就是重试风暴的信号——正确拆法是：超时预算给足单次调用，重试次数压到最低，慢调用的兜底交给上层按行补跑（失败粒度从"全场"降到"单行"，补跑成本也随之降低）。

---

## 问题 8：eval 共用 session_id，chat_history 污染检索导致负例漏拒

**现象**：8-14 全量档拒答准确率从 1.0 退化到 **4/6**——「你好」rerank 分 0.9988、「asdfqwer123」0.9616（其余负例 0.0002~0.0016 正常拒），两条都一本正经答了清洁方法还带 `[4]` 引用。8-11 验证过的完全可分（0.780 ≫ 0.010）被打破。诡异在：**检索链一个字没改**。

**根因**：eval 里所有问题共用一个 session_id，而查询图的 `item_name_confirm_node` 会按 session_id 从 Mongo 拉最近 10 条 chat_history 拼进问题改写/检索链：

```python
# 修复前：11 条问题全部共用同一会话
query_state = {
    "original_query": question,
    "session_id": "eval_plus_session",   # ← 共用
    ...
}
```

eval 先跑可回答集（全是 HAK180 问题），这些 QA 对写进了 Mongo；轮到负例「你好」时，查询链的输入不再是孤零零的"你好"，而是「HAK180 历史问题 + 你好」——历史把检索带偏到安全手册，reranker 给出高分，闸门失效。佐证：日志里「你好」条目 `chat_history: 10 条记录` 全程非零。**单轮评测被多轮会话机制污染**。

**修复**：每条问题独立会话（uuid）：

```python
"session_id": f"eval_plus_{uuid.uuid4().hex[:8]}",  # 每条独立会话：QA 集是单轮问题，历史必须为空
```

**验证**：修复后全量档拒答 6/6 全对——「你好」0.0044、「asdfqwer123」0.0101，阈值恢复完全可分 0.780 ≫ 0.010；检索分数与 8-11 档逐条一致（检索链本身确实没变，变的只是输入）。Config A 据此定稿。

**教训**：**评测环境必须显式声明它的隐含前提**。QA 集是单轮问题，前提是"无历史"——但系统里存在按 session_id 注入历史的机制时，这个前提不会自动成立。凡是有会话记忆的系统，跑单轮评测前先问一句：这条查询真正喂进检索链的输入是什么？（和问题 5 的教训同源：**诊断看 LLM/检索的真实输入，别看你以为的输入**。）

---

## 问题 9：is_refusal 散词匹配把正常回答的"资料不足部分"误判成拒答

**现象**：9-1 定稿档复盘发现 Q3「清洁方式」`is_refusal=True`，但它的答案是完整正确的回答——eval 摘要会误报"误拒 1/5"（实际 0/5）。

**根因**：`is_refusal` 用散词子串匹配：

```python
# 修复前
REFUSAL_MARKERS = ("资料不足", "暂无法", "低于阈值")
return any(m in answer for m in REFUSAL_MARKERS)
```

Q3 答案末尾有一句**正常陈述**：「**资料不足**部分：参考内容未提及除干抹布外的其他清洁剂……」——这是能力⑤要求的"指出缺失信息"，行为是对的，却撞上了 marker。"资料不足"作为散词，在正常回答里出现完全合法。

**修复过程中的弯路**：第一反应是 `any()` → `all()`（三个词全中才算拒）。但穷举拒答文案的**全部发射点**后发现 `all()` 会引入假阴性——系统里拒答文案有三个模板，并不都含三个词：

| 发射点 | 文案开头 | 资料不足 | 暂无法 | 低于阈值 |
|---|---|---|---|---|
| `answer_node.py` 空召回 | 「资料不足，暂无法回答该问题。……」 | ✓ | ✓ | ✗ |
| `answer_node.py` 低于阈值 | 「资料不足，暂无法可靠回答……低于阈值 0.4……」 | ✓ | ✓ | ✓ |
| `query_prompt.py` LLM 判资料不足 | 「资料不足，暂无法回答该问题。」 | ✓ | ✓ | ✗ |

`all()` 只覆盖第 2 条路；当条负例恰好全走第 2 条路时 `all()` 碰巧全对，但哪天某条负例召回为空走了路 1，真拒答就被记成"作答"——潜伏 bug。三个模板的**公共结构**是连续短语「资料不足，暂无法」开头，而 Q3 那句是「资料不足**部分：**」，不构成该短语：

```python
# 修复后：判连续短语，与三个模板的公共开头对齐
REFUSAL_MARKER = "资料不足，暂无法"
def is_refusal(answer):
    return bool(answer) and REFUSAL_MARKER in answer
```

**验证**：`--no-ragas` 回归——摘要"误拒"行消失、拒答 6/6、阈值 0.780 ≫ 0.010 不变；全量定稿档 Q3 正常作答且五指标齐全。

**教训**：**改判定规则前，先穷举所有"真相发射点"**。`any()` 是在赌"词不共现"，`all()` 是在赌"词总共现"——两者都是在模板集合上碰运气；可靠的判据来自模板的公共结构（这里是不变的短语开头），而不是词的出现组合。已知边界（记录给未来）：裸 Qwen（Config B）指令遵从弱，可能不按模板说话（如「抱歉，这个问题我不清楚」），届时拒答指标若反常好，先怀疑规则漏计，必要时升级为 LLM 判拒答。

---

## 问题 10：云 GPU 环境搭建四坑（镜像挂起 / HF 拉不动 / ModelScope 嵌套路径 / transformers API 变更）

**背景**：微调第 1 课——智川云 RTX 4090（镜像 PyTorch 2.11.0-CUDA12.8-Devel-Py3.12-Ubuntu24）装 LLaMA-Factory + 下 Qwen2.5-0.5B-Instruct。计划五步：①GPU 验证 ②装 LF ③CLI 验证 ④下模型 ⑤推理冒烟。①④⑤ 之外的每一步都踩了坑，四个坑连着看有一条主线：**国内访问境外源（GitHub/PyPI/HF）全线不稳，且各家工具的"你以为的路径/API"和实际不一致**。

**坑 a：ghproxy 镜像 git clone 挂起**。`git clone https://mirror.ghproxy.com/https://github.com/hiyouga/LLaMA-Factory.git` 跑 1 分钟无任何输出（不是慢，是挂死）。
**解法**：本地浏览器下载 GitHub 仓库 zip → 上传服务器解压 → 照常 `pip install -e ".[torch,metrics]"`。`-e` 开发模式装的就是解压目录，功能不受影响。
**教训**：镜像站挂起时别干等——判断依据是"有无输出"而非"快慢"，60s 零输出就换路。浏览器下载+上传是命令行不通时的万能兜底。

**坑 b：hf-mirror 拉不动模型**。`HF_ENDPOINT=https://hf-mirror.com` + `huggingface-cli download` 在该机器上不稳定。
**解法**：改用 ModelScope（魔搭，国内源）：
```python
from modelscope import snapshot_download
model_dir = snapshot_download('Qwen/Qwen2.5-0.5B-Instruct', cache_dir='./models', revision='master')
```
**教训**：国内下模型首选 ModelScope 而不是 HF 镜像——前者是阿里自己的 CDN，后者是社区公益镜像，稳定性不在一个量级。

**坑 c：ModelScope 落盘路径嵌套，config.json 不在 cache_dir 根下**。`cache_dir='./models'` 下载完成后，`./models/` 下直接找不到 `config.json`——报 `OSError: ... is not a valid model path` 一类错误。
**根因**：ModelScope 的 cache 结构是 `cache_dir/<模型名>/snapshots/<revision>/`，真实模型文件（config.json、*.safetensors）在最内层 `snapshots/master/` 下，中间层放的是 blobs 和元数据。
**解法**：所有引用模型的地方（冒烟脚本、训练 yaml 的 `model_name_or_path`）都用完整路径：
```
/root/rivermind-data/models/Qwen2.5-0.5B-Instruct/snapshots/master
```
**教训**：下载工具"下载到哪"和"文件真实在哪"是两回事——报"not a valid model path"时先 `ls` 到 `config.json` 那层为止，把那层的绝对路径拿来用。（备查：想避免嵌套，重新下时用 `local_dir=` 参数。）

**坑 d：冒烟脚本的 transformers API 两处变更**。初版冒烟脚本直接报错。
- `tok.apply_chat_template(messages, return_tensors="pt")` 返回的是 **BatchEncoding 对象**（非裸 tensor），没有 `.to()`——要取 `.input_ids` 再搬 CUDA；
- `AutoModelForCausalLM.from_pretrained()` **没有 `device=` 参数**——正确写法是 `device_map="auto"`。

修复后的冒烟脚本（留档项目根 `smoke.py`）：
```python
inputs = tok.apply_chat_template(messages, add_generation_prompt=True, return_tensors="pt")
input_ids = inputs.input_ids.to("cuda")          # ← 取 .input_ids，不能对 BatchEncoding 直接 .to()
model = AutoModelForCausalLM.from_pretrained(MODEL_PATH, torch_dtype="auto", device_map="auto")  # ← device_map 而非 device
out = model.generate(input_ids, max_new_tokens=64)
print(tok.decode(out[0][input_ids.shape[1]:], skip_special_tokens=True))  # ← 切掉输入，只解码新生成部分
```
**教训**：transformers 的 API 在快速迭代，"从记忆里写代码"容易撞上返回类型/参数签名变更——报 AttributeError/TypeError 时先怀疑"接口变了"，查当前版本文档而不是改逻辑硬凑。

**验证**：①`nvidia-smi` + `torch.cuda.is_available()` 绿；②LF 装上、CLI 待最终复核；④模型文件在 `snapshots/master/` 齐全；⑤冒烟脚本修复后待最终输出确认（下一节课开机复核 ③⑤，全绿后关机）。

**目录约定（数据盘，关机不丢）**：一切装 `/root/rivermind-data/` 下（系统盘 /root 根下不放东西，防重置清空）：`LLaMA-Factory/`、`models/`、`data/`、`outputs/`。

---

## 问题 11：手写 SFT 数据的格式陷阱（句级编号 / 分隔符 / 考卷污染 / GBK 编码）

**背景**：微调第 2 课——Windows 本机手写 HAK180 手册的 SFT 数据（CSV 五列：type/item_names/questions/contexts/answer），正例带 [n] 引用、负例用拒答模板。首版 `eval/microAdapter/SFT_Questions.csv` 连踩四坑。

**坑 a：引用编号体系自创，与生产不同构（最关键）**。首版把 [1][2][3] 写进 context **内部**当**句子**编号，answer 里的 [n] 指句子。
**正确做法**：生产里编号是**段落级**的——`answer_node.py` 的 `format_reranker_docs()` 把每段包装成 `[index]:{meta}\n<内容>`（answer_node.py:139），模型看到的编号是段序号。所以训练数据必须：contexts 用 `---`（三横）**单独一行**分段、段内是纯手册原文；answer 的 [n] **从 0 数、指向段落**。
**教训**：SFT 数据格式不是"怎么清楚怎么来"，而是**训练格式=推理格式**——模型上线时 answer_node 会怎么拼 prompt，训练数据就得长什么样，否则教的是一套上线后永远见不到的格式，白教。

**坑 b：contexts 里混进了手写答案句**。首版第 1 行 context 末尾带了一句自己写的"不能用酒精擦[1]"——contexts 必须纯手册原文，一个自己的字都不能加（答案句混进 contexts = 教模型"从问题上抄答案"）。

**坑 c：正例与 eval 考卷撞车**。首版第 1 行"酒精清洁"正是 eval qa.csv Q3 考的事实——训练集教会它，将来 Q3 分数分不清是"模型会答"还是"背过答案"，B/C 对比被污染。
**教训**：铁律的具体执行——写每条正例前对照 qa.csv 五题（卡纸、加热器/空调、酒精清洁、起搏器、桌面边缘/出纸盒/电源插座）和 qa_neg.csv 六题（你好/笑话/乱码/天气/iPhone/餐厅），**撞事实的换小节重写**，近似换说法也不行。

**坑 d：CSV 存成了 GBK**。Excel/Windows 记事本默认 ANSI/GBK，pandas/LLaMA-Factory 按 UTF-8 读全是乱码。
**解法**：VS Code 右下角 → Save with Encoding → UTF-8（或 Excel 选"CSV UTF-8"）。

**其他小项**：①item_names 笔误写成不存在的 HAK181；②引用标号跑到句号外面（`…触电。[0]`）——生产规则是标号贴事实句**末尾、句号前**（对照 ANSWER_PROMPT 示例"红表笔接正极、黑表笔接负极[0]"）；③分隔符写成两横 `--`——必须三横 `---` 且单独一行。

**合格样例**（段间 `---` 换行、标号句号前、[n] 指段、结论先行）：
```csv
pos,HAK180烫金机,可以拆解本设备么？,"拆解本设备可能会导致火灾或触电。
---
请勿拆解本设备。","请勿拆解本设备[1]。拆解本设备可能会导致火灾或触电[0]。"
```

**澄清过的认知**：造 SFT 数据**不碰 Milvus/向量库**——contexts 是手动模拟"检索正常工作会返回什么"，标号是自己排的段序，不是从库里召回的。

---

## 问题 12：照抄基准错位——PDF 原文 ≠ Milvus 里的 chunk 文本

**现象**：按 PDF 视觉版式手写 contexts，自认为"逐字原文"，但与线上检索返回的文本系统性对不上：小标题"设备"在 chunk 里不存在（被解析器吃成空 `#` 标题）、标题实为 markdown 形式 `# 重要事项`、bullet 带 `•\t` 前缀、空格被归一（`Brother不建议` 无空格、`注意/ 警告` 斜杠后多空格）、一处 `Brother 经\n销商` 被 PDF 断行、chunk[7] 三条 bullet 连排一行用行中 `•\t` 分隔、LaTeX 温度符号 `$1 0 ~ ^ { \circ } \mathsf { C }$` 带怪异空格。

**根因**：**照抄基准搞错了对象**。PDF 是"人眼看到的原文"，模型推理时读到的却是解析管线产出的 chunk（PDF→markdown→切分入库），两者是经纬分明的两种文本。按 PDF 抄 = 造生产里不存在的"干净"文本，违反训练格式=推理格式。另一个认知纠偏：**"造数据不碰 Milvus"指的是不用向量检索来造答案，不是不能读 chunk 拿真值**——chunk 恰恰是 contexts 的照抄基准。

**修复**：
1. 找到现成基准：HAK180 入库切片文件就在仓库 `knowledge/front/temp_data/20260804/78dc0628-08d8-4d5e-8eb4-bfe3a5ed14f8/chunks.json`（16 个 chunk 全量，title/content/file_title/parent_title 四字段）——不用连 Milvus。
2. 写 `eval/microAdapter/dump_contexts.py` 把 16 个 chunk 导出成 txt，写数据时对着复制。
3. 约定：**段 = chunk 全文的连续子串**（允许去行首 `•\t` 与行尾空格）；标题行可选、保留必须 `# xxx` 原样；**裸标题（不带 # 的"设备"）禁止**。

**验证**：`validate_sft.py` 的原文比对从 18 段未命中（含校验器自身 bug）修到全绿。

**教训**：**"原文"是个相对概念——先问"谁的原文"**。数据链路里同一份内容有多个形态（PDF→markdown→chunk→prompt 拼接），照抄前先锁定"模型推理时读到的形态"是哪一环。判断依据不是哪个"更干净/更规范"，而是哪个在生产链路上。

---

## 问题 13：肉眼对不了空格——逐字校验必须机器做

**现象**：77 条数据人工检查多轮"看着没问题"，机器校验仍抓出一堆偏差：`Brother 经销商` 被拼回正常句子（chunk 里是 `经\n销商` 断行）、chunk[7] 行中 `•\t` 连排被丢、LaTeX `\mathsf {C }` 丢了一个空格。全是"顺手修正"型错误——手写者无意识地把 PDF 的视觉规整还原了。

**根因**：两个。①人眼对空格/换行/标点的敏感度远低于对文字内容的敏感度，逐字照抄靠肉眼必然漏；②校验器首版自身有 bug——`seg in chunk_texts` 是**列表成员判断**（整段等于某个 chunk 才算命中），不是子串查找，导致 18 段假阴性。段是 chunk 内的连续片段，正确写法是拼一个全量大文本做子串判断：`seg in chunk_full`。

**修复**：
1. 校验器改为 `chunk_full = '\n\n'.join(所有 chunk 归一化文本)`，段命中 = `normalize(seg) in chunk_full`；归一化只去行首 `•\t`/行尾空格/压 3+ 连换行。
2. **偏差定位技巧：最长公共前缀二分**。对未命中段，二分找它与 chunk 全文的最长公共前缀长度 lo，分叉点就在 lo 处——打印 CSV 与 chunk 各自 lo 前后 20 字，一眼看出是断行、`•\t` 还是空格问题。比肉眼 diff 快且准。
3. `eval/microAdapter/validate_sft.py` 终版校验项：UTF-8 解码 / `---` 切段 / pos 引用编号不越界且无裸奔句 / neg 以"资料不足，暂无法回答该问题。"开头且无引用 / question 与 qa.csv、qa_neg.csv 查重 / 段=chunks.json 连续子串。加数据就重跑。

**验证**：76 条全绿（pos 47 / neg 29）。

**教训**：**"逐字照抄"类要求，验收必须机器化**——人写的就用人查不出来的方式错。同时校验器自己也要过一遍"它真的在查我以为它在查的东西吗"（`in list` vs `in str` 一字之差，语义全反）。

---

## 问题 14：CSV→alpaca 转换——训练集如何对齐推理格式（含 neg 分数闸门）

**背景**：第 2 课收尾。`eval/microAdapter/convert_to_alpaca.py` 把 `SFT_Questions.csv` 转成 LLaMA-Factory 用的 alpaca（instruction/input/output），产出 `sft_alpaca.json`（76 条）+ `dataset_info_sft.json`（注册片段）。几个关键决策各有线上出处：

1. **instruction 单源加载线上 ANSWER_PROMPT**：用 `importlib` 从 `knowledge/prompts/query/query_prompt.py` 现读 `ANSWER_PROMPT` 填空，不复制粘贴——线上 prompt 改了，重跑脚本即同步，永不漂移。
2. **参考内容嵌在 instruction，input 留空**：线上生产就是 `build_prompt()` 把模板+context+问题 format 成**一整条 user 消息**（无 system 角色，llm_client_util.py 用 ChatOpenAI 直接 invoke 整段），所以训练样本同样整段进 instruction。alpaca 的 input 字段为空串。（易困惑点：看 JSON 时以为"没有参考内容"——其实在 instruction 的【参考内容】~【历史对话】之间。）
3. **段包装复刻 answer_node.py:139**：`{context}` 渲染为 `[n]:{'score':.., 'url':.., 'title':..}\n<段原文>`，段间 `\n\n`；title 从 chunks.json 反查（含 `# ` 前缀）；url 留空（生产 `doc.get("url","")` 默认空）。
4. **空 contexts → "无参考答案"**：对齐 answer_node.py:98 空检索时的渲染，与 ANSWER_PROMPT 规则 3 呼应。CSV 里 neg 空 contexts 就写空，不手写这四个字。
5. **合成 rerank 分数的两条纪律**：
   - **确定性**：md5(question+i) 做种子合成，重跑结果不变——训练数据可复现，避免随机数导致两次转换产物不同。
   - **neg 带段条目 top 分必须 ≥ 0.4 拒答闸门**（`check_context_sufficient` 的阈值）：neg 带段教的场景是"过了闸门但内容答不了 → LLM 第二道防线拒答"；若合成分低于 0.4，线上这种 prompt 根本到不了 LLM（闸门直接硬拒），等于教一个永远不会出现的场景，白教。
6. **{question} 渲染为"HAK180烫金机：{问题}"**：对齐线上 rewritten_query"包含商品名称的独立完整问题"形态（与 eval qa.csv 前缀同款）；{history}="暂无历史"（对齐 answer_node.py:99）；{item_names}=str(['HAK180烫金机'])（生产 format(list) 的字面形态）。
7. 转换内置自检：占位符残留 assert（`{context}` 等四个花括号没填上就炸），76 条零残留。

**产出数据形态**：pos top 分 ~0.9 档逐段递减（对齐 eval 可回答集 top 0.78+ 的真实分布）；neg 带段 12 条 top 分 0.62~0.658（全部过闸门）。instruction 长度 597~1043 字。

**教训**：转换脚本的每一处"看起来随意的决定"都应该能指到一行生产代码——指不到的（比如"分数量级怎么定"）就指到 eval 存档的真实分布。**数据工程里没有"差不多就行"，只有"有出处"**。

**下一步（第 3 课）**：开机复核第 1 课遗留 ③`llamafactory-cli train --help` ⑤`python smoke.py` → 裸 0.5B 推理定性基线（抽 sft_alpaca.json 几条 pos/neg 的 instruction 直接喂裸模型记录输出）→ 第 4 课 LoRA SFT。云端需拷 `sft_alpaca.json` + `dataset_info_sft.json`（合并注册进 LLaMA-Factory/data/）。

---

## 问题 15：判据又撞模板文本——"无参考答案"四个字每条 instruction 里都有

**现象**：`bare_baseline.py` 抽负样本时 `negs_ctx` 永远是空列表，`negs_ctx[0]` 抛 IndexError。用户按报错反推"数据集里没有带段负例"——但数据实际有 12 条。

**根因**：过滤条件写的是 `"无参考答案" not in instruction`——**每一条** instruction 都含这四个字，因为它是 ANSWER_PROMPT 规则 3 的模板文本（"若【参考内容】为'无参考答案'，或与问题无关……"）。散词子串判断在"该词是 prompt 模板固定成分"的场景下必然失真。

**修复**：判据锚定**结构位置**而非散词——空 contexts 的特征是这四个字紧跟在【参考内容】标题后（`"【参考内容】\n无参考答案" in instruction`），带段负例反之。顺带对两个抽样列表都加了空列表 `SystemExit`（明确报错）替代裸 IndexError。

**教训**：这与问题 9 的 is_refusal 假阳性**完全同源、第三次踩**（#9 散词"资料不足"、#11 坑 b contexts 混答案句、本例散词"无参考答案"）。规律已经清楚：**凡是要在"含 prompt 模板的文本"里判断语义特征，判据必须锚定结构位置（标题后/行首/固定搭配），不能用裸散词**——prompt 模板里的散词是"所有人都有"的，不构成区分度。写这类判断前先问：这个词会出现在不该匹配的样本里吗？

---

## 问题 16：LLaMA-Factory qwen template 默认加 system——训练/推理消息结构的隐性不同构

**现象**：第 4 课训练日志的 `training example` 打印显示，每条样本的实际输入是：

```
<|im_start|>system
You are Qwen, created by Alibaba Cloud. You are a helpful assistant.<|im_end|>
<|im_start|>user
（我们的整段 ANSWER_PROMPT 填空 instruction）<|im_end|>
<|im_start|>assistant
（answer）<|im_end|>
```

而裸基线脚本（apply_chat_template 单 user 消息）和生产（ChatOpenAI 直接 invoke 整段 prompt，无 system）都没有这个 system 段——训练与推理的消息结构差一截。

**根因**：LLaMA-Factory 的 `template: qwen` 自带默认 system prompt（不配置 `default_system` 参数时注入 Qwen 官方默认）。我们设计的"instruction=单条 user 消息"被 template 渲染成了 system+user 两段。

**决策：不重训**。三条理由：①看 label_ids——system 段全部标 -100，不参与 loss，模型没有被教"这个 system 内容"；影响仅是推理时少一个前缀，方向是"训练多见、推理少见"，属于轻度分布差异而非格式错位（对比问题 11 坑 a 那种 answer 编号指向不存在体系的硬伤）。②练手轮的目的是验证"SFT 能否教会引用/拒答/忠实三行为"，after 对比脚本补上同样的 system 消息即与训练分布对齐，可干净验证。③如果 after 表现好，说明该差异不伤害行为习得；7B 正式轮再配置 default_system 对齐生产。

**教训**：**用框架 template 时，先看它渲染出来的真实样本再训练**——训练日志里的 `training example`（input_ids/inputs/labels 三段打印）就是干这个的，这次就是从里面发现的。框架的"默认值"（system prompt、自动 bos/eos、截断行为）都可能在你不注意的地方改变消息结构。"训练格式=推理格式"要贯彻到 chat template 粒度，不只是 prompt 文本粒度。

**附：训练日志健康读数（第 4 课，2026-09-02 21:18-21:19）**：76 样本/3epoch/30 步/29 秒；loss 1.044→0.6266→0.4018→0.3319→0.3354→0.2643 平滑收敛；trainable 4,399,104 (0.88%)；grad_norm 3.5→1.6 无爆炸。完整日志留档 `eval/microAdapter/log.txt`，分析见项目根 `微调过程记录分析.md`。

---

## 问题 17：DPO 偏好对的四个坑——跨题舀水 / 形态与配方错位 / 行尾双空格 / 目录重组路径断

**背景**：第 5 课手写 DPO 偏好对（`DPO_Questions.csv`，七列含 flaw 配方标签）。用户写 8 条 + Claude 扩 24 条，校验脚本（validate_dpo.py）开发与数据撰写交替进行，四个坑按发现顺序记录。

**坑 a：跨题舀水（两次，人一次 AI 一次）**。注水配方的 rejected 要在 chosen 后追加一句"真实但没被问"的话，铁律是**追加句必须取自本题 contexts**。用户首写在"堆放杂物"题里搬了"烫金膜盒"句（来自另一题的 contexts），Claude 扩批在"电源线压东西"题里搬了 chunk_06 的"拉伸或拉紧电源线"句——都挂了本题的 `[n]` 标号，指向不含这句话的段。后果：一对里同时混入注水+虚假引用两个缺陷，模型说不清你在教什么。**两次同类错误（一次人工一次 AI，间隔一轮对话）证明这类检查必须机器做**：validate_dpo 的注水检查=前缀+追加形态下，追加句按 6 字滑窗在所指标段找锚点，找不到报错。收编真实模型输出的行豁免（其标号本来就错，那是训练信号的一部分），改为"句内容在全部段里任一段命中"的宽松溯源。

**坑 b：rejected 形态与 flaw 配方错位**。"幻觉编造"配方的 rejected 必须是编造内容（自信给数字/功能），不能还是拒答模板——"内存容量"一条初版写成"资料不足……手册未提及"，决策没变就不构成"幻觉 vs 拒答"的对比，白占一对。同款问题的弱化版："拒答理由含糊"一对 chosen/rejected 只差几个字（"耗材更换周期相关内容"vs 空泛），对比度太弱梯度信号稀薄，升级为幻觉编造配方。**判据：一对 rejected 拿掉 flaw 标签后，你自己能不能说出它比 chosen 差在哪——说不出的就是对比度不足**。

**坑 c：行尾双空格也是照抄基准的一部分**。"必须\n按照使用说明书正确安装"段在 chunk 里行尾有两个空格（markdown 硬换行），SFT 版被 validate_sft 的行尾归一放行了，DPO 逐字节子串校验查了出来。修复=按原文补回。**连带发现**：同一题在 SFT/DPO 两个数据集的 instruction 必须字节相同（md5 确定性分数的设计），该段此前 SFT 版少了双空格导致两数据集同题 instruction 差 2 字节——把 SFT_Questions.csv 同段对齐后重转两侧，28 复用题 28/28 字节一致。教训延伸问题 13：**肉眼对不了的空格，连"从已校验文件复制"都会带进差异，归一化放行的东西在做字节级同构时是坑**。

**坑 d：目录重组后相对路径断**。脚本收进 `scripts/` 子目录后，`HERE = Path(__file__).parent` 语义变化：数据文件（CSV/JSON）在上一层（`HERE.parent`），知识库（chunks.json/query_prompt.py）在项目根（`HERE.parent.parent.parent.parent`）。7 个脚本 3 类路径逐一修，修完跑全链路回归（validate_sft/validate_dpo/convert_to_alpaca/convert_to_dpo 四脚本全绿）。**教训：目录结构调整后，凡用 `__file__` 相对定位的脚本必须跑一遍冒烟，肉眼 review 不了路径层级**。

**成果**：32 对（幻觉编造 11 / 引用错位 8 / 注水 7 / 拒答理由含糊 6；pos 15 / neg 17；hard negative 6 条全在拒答理由含糊）→ `dpo_preference.json` + `dataset_info_dpo.json`（ranking:true）。chosen 走 SFT 全套老规则；rejected 按配方机器查。第 6 课开机即训。

---

## 问题 18：DPO 训练指标漂亮、生成质量倒退——过矫正与指标脱钩

**现象**：第 6 课 DPO 训练指标全线健康（accuracies 0.81→0.94、margins 1.49→2.86、logps/chosen 稳定不崩、loss 0.39→0.15），但 after_dpo.json 同 6 条生成对比：pos 引用正确 1/3→0/3，新增 1 例假拒（可回答问题答"资料不足"），idx 2 大段答非所问——**净伤害，回退用 SFT checkpoint**。

![image-20260903212619036](C:\Users\oprah\AppData\Roaming\Typora\typora-user-images\image-20260903212619036.png)



![image-20260903212800052](C:\Users\oprah\AppData\Roaming\Typora\typora-user-images\image-20260903212800052.png)

**根因（三个证据同一诊断——DPO 过矫正）**：

1. **配比失衡**：32 对里 17 对拒答侧（幻觉编造 11 + 拒答理由含糊 6），reward 梯度把模型整体往拒答方向推。假拒句式"手册只说明X，但未涉及Y"正是 hard negative 配方里 chosen 的模板——**模型把配方句式泛化到了可回答问题上**。教训：配比不是"越多越好"，neg 侧占比高=在教"多拒答"，正如 pos 侧占比高=在教"多硬答"。
2. **间隔靠打压 rejected 拉开**：logps/rejected 从 -22.7 压到 -33.5，chosen 稳在 -14——margins 变宽主要来自"把 rejected 打下去"而非"把 chosen 抬起来"，生成分布整体受损（idx 2 答非所问即表现）。
3. **训练指标量错了东西**：accuracies/margins 量的是"训练对上分得开优劣"，不是"held-out 生成质量"。32 对小数据上把这两者分开极易（把 rejected 的 token 概率压低就够），于是指标满分、模型报废。

**处置**：0.5B 练手轮不迭代（低收益内卷），最佳 checkpoint 定格 SFT 版。DPO adapter 与 after_dpo.json 留档作反面教材（`sft_output_0.5B/dpo/`）。

**带去 7B 正式轮的四条**：①DPO 配比反转——引用类 pos 对 ≥ 拒答类 neg 对（或拒答对减半）；②epoch 3→1、lr 5e-5→2e-5 起步；③验收看 held-out 生成对比，不看训练 accuracies；④SFT checkpoint 永远保留，DPO 是有风险的手术要能回退。

**附：云脚本路径小坑**：after_dpo.py 上传时放错目录（LLaMA-Factory/ 根而非 data/），`HERE.parent / "sft_alpaca.json"` 解析到不存在的路径。云端惯例=脚本平放 data/ 里与数据同目录（bare_baseline/after_sft 同款），本地 scripts/ 子目录的 `HERE.parent` 语义在云端不成立。**上传云端脚本时想一下 HERE 在那边指哪里**。

---

## 问题 19：Reranker 精排 78s 疑云——算力物理账与三重优化（线程引导 / max_length / 模型 A/B）

**现象**：lumy 链路 bge-reranker-large（560M，fp32 CPU）精排 8 条 pair 耗时 70~78s，是查询链路最大瓶颈。排查期间的三个干扰信号：CPU 仅 35~40%（核没用满？）、任务管理器内存 96%（内存不足？）、外部大模型给的解释"Windows + PyTorch CPU 原生跑 bge-reranker-large 本身性能差"（玄学结论，不可操作）。

**排查过程（排除法，四项假设逐一证伪）**：

| 假设 | 验证方法 | 结论 |
|---|---|---|
| OMP/MKL 线程 env 没生效 | env 移到 import torch 之前 + 打印 `torch.get_num_threads()` | =4 确认生效，排除 |
| token 超长触发 512 padding | 打印每组 query+doc 的 token 长度 | 多数不足 512，排除 |
| 循环逐条调用而非批量 | 审查 `compute_score` 调用点 | 一次性传全部 pair，排除 |
| 内存不足 | 汇总各进程内存（合计~3.8GB）对照任务管理器总数 | 96% 是 Windows 备用缓存假象，排除 |

**根因**：**算力物理账，不是 bug**。560M 参数交叉编码器 × 8 pair × ~500 token ≈ 3 TFLOPs 计算量；i5-11320H（4C8T 笔记本，无独显）fp32 CPU 4 线程有效算力 ~35 GFLOPS → 3 TFLOPs ÷ 35 GFLOPS ≈ 78s，与实测完全吻合。CPU 35~40% 的真相：4 线程 ÷ 8 逻辑核 ≈ 50%，扣掉串行段正好是观测值——**核没用满本身就是线索（限制太狠），不是异常**。

**修复**（三项叠加：78s → 线上实测 5.1~7.2s，约 12x）：

(1) **线程 4→8 + env 引导收口到进程入口**。torch 由 FlagEmbedding 间接 import，OpenMP/MKL 运行时在 import torch 那一刻就初始化并读走线程数，之后再设 env 无效——项目里没有直接的 `import torch`，env 必须落在"任何 knowledge.* 子模块 import 之前"必执行的位置，即包 `__init__.py`（一处覆盖 API/eval/节点 `__main__` 全部入口）：

```python
# knowledge/__init__.py —— 全项目最早的执行点，只允许 import os
import os
# CPU 并行线程数限制：必须在 import torch 之前执行
os.environ["OMP_NUM_THREADS"] = "8"
os.environ["MKL_NUM_THREADS"] = "8"
os.environ["OPENBLAS_NUM_THREADS"] = "8"
os.environ["VECLIB_MAXIMUM_THREADS"] = "8"
os.environ["NUMEXPR_NUM_THREADS"] = "8"
```

(2) **max_length 512→256**。交叉编码器算力 ∝ 序列长度²，截断收益是平方级。config 加开关、调用点接线：

```python
# config.py —— 交叉编码器算力∝长度²：512→256 实测 32.7s→16.4s（i5-11320H 8线程）
rerank_max_length: int = field(
    default_factory=lambda: int(os.getenv("RERANK_MAX_LENGTH", "256"))
)

# multi_search_rerank.py —— 原调用没传 max_length（一直吃默认 512），接线：
reranker_score = reranker_model.compute_score(
    sentence_pairs=query_doc,
    normalize=normalize,
    max_length=self.config.rerank_max_length)
```

(3) **模型 large→base A/B 转正**。bge-reranker-base（278M）单项提速 3.8~4.1x，`.env` 开关式切换、可一行回退：

```python
# bge_rerank_util.py —— BGE_RERANKER_PATH 优先，注释即回退 large
model_path = os.getenv("BGE_RERANKER_PATH") or os.getenv("BGE_RERANKER_LARGE")
```

**验证**（每个解法独立验证 + 端到端回归）：

1. **线程**：`torch.get_num_threads()` 打印 =8；基准脚本同负载扫线程（scripts/bench_reranker_threads.py，8 条 pair × ~548 token）：4 线程 38.3s → 8 线程 32.7s（1.17x，HT 收益符合预期上限）。
2. **max_length**：同基准 512→256：32.7s→16.4s（1.8x）；**分数侧验证**：示例 logit 3.963→3.862，sigmoid 后 0.981 vs 0.977——偏差 0.4%，远小于拒答阈值 0.4 的安全边际，可忽略。
3. **base 模型**：同基准 ml=256 仅 4.36s（large 的 3.8x）；模型加载 1.7s、278M 参数核对无误。
4. **端到端 A/B（质量的最终裁判）**：`lumy_eval` 13 题自测 **13/13 = 100%，与 large 基线完全一致**（基线存档 eval/lumy_result.md，base 结果 eval/lumy_result_base.md）；**拒答阈值安全性专项**：从 logs/日志.log 的 `精排完成 | Top5分数` 读分布——拒答题（价格/库存）Top1 ≤ 0.17，事实题 ≥ 0.42，阈值 0.4 两侧边际清晰，**换模型后阈值无需重校准**；精排节点耗时 5.1~7.2s（此前 large 时代 ~50s）。

**基准数据汇总**（8 条 pair × ~548 token，8 线程）：

| 配置 | large (560M) | base (278M) |
|---|---|---|
| ml=512 | 32.7s | 7.98s |
| ml=256 | 16.4s | **4.36s** |

**教训**：
1. **性能问题先算物理账再找 bug**：FLOPs 估算 ÷ 平台有效算力 ≈ 实测耗时，就是 compute-bound 实锤——这一步 5 分钟，能省掉后面所有"是不是我代码写错了"的瞎猜。外部大模型的"Windows 就是不行"属于不可操作的玄学结论， distrust 并拆解之。
2. **env 类"必须最早生效"的设置，落点是包 `__init__.py`**，不是各入口文件顶部——入口有 N 个（API/eval/节点调试/临时脚本），靠纪律逐个贴必然漏。`torch.set_num_threads()` 可运行时改（env 只在 import 时读一次），基准脚本靠这一点在一个进程内扫多组线程数。
3. **hf-mirror 的 LFS 大文件下载坑**（呼应问题 10 坑 b）：`snapshot_download` 对大文件报 "Distant resource does not seem to be on huggingface.co"——镜像重定向响应缺 huggingface_hub 校验所需的元数据头，**小文件能过、大文件必挂**。绕法：大文件用 `curl -L -C -` 直连 resolve 地址（普通 GET 不做头校验且可断点续传）。
4. **换 reranker 模型 = 换分数分布**，拒答阈值必须重新核对而不是沿用——验证方法是看日志里 Top5 分数的两群体分离度（拒答群体上限 vs 可答群体下限夹住阈值）。本次 base 分离度健康故阈值未动；若漂移，先调阈值再对比，否则拒答率的回归是假的。
5. **三变量同时上线（线程/截断/模型）要有隔离预案**：线程不影响分数可豁免；若质量回归，先注释 `BGE_RERANKER_PATH` 回 large 保留 ml=256 跑一次，即可区分"模型的锅"还是"截断的锅"。本例一次通过未触发，但预案先写在 A/B 流程里。

---

## 变更日志

- **2026-08-11**：建文档。汇总问题 1–5（prompt+闸门、eval_plus、web 关闭、reranker 归一化、上下文预算扩容）。预算修复当晚验证通过（Q2/Q3 解拒、零回归）。
- **2026-09-01**：补问题 6–9（embeddings wrapper dict、重试风暴、session_id 污染、is_refusal 假阳性）。同日 **Config A 基线定稿存档**于 `eval/archive/configA_2026-09-01/`（全量 RAGAS：faithfulness 0.935 / citation 1.0 / refusal 6/6 / 阈值完全可分 0.780≫0.010）。下一步进入微调阶段（0.5B 练 SFT/DPO → Qwen2.5-7B 抓 B → 微调抓 C）。
- **2026-09-01 晚**：补问题 10（云 GPU 环境四坑：ghproxy 挂起→zip 上传、hf-mirror→ModelScope、snapshots/master 嵌套路径、transformers BatchEncoding/device_map）。更正：Qwen2.5 系列最小 0.5B，无 0.6B（此前笔误），练手模型定为 Qwen2.5-0.5B-Instruct。
- **2026-09-02**：补问题 11（手写 SFT 数据四坑：句级编号→段落级、contexts 混入手写句、正例撞考卷、GBK 编码）。第 2 课启动：用户已写出首条合格样例，继续写至 ~20 条后交由校验脚本（格式/越界/考卷查重/pos-neg 比例）+ CSV→alpaca 转换（instruction 复刻 ANSWER_PROMPT、contexts 按生产 `[0]:{...}` 包装）。
- **2026-09-02 晚**：补问题 12–14，**第 2 课数据全链路闭环**。12=照抄基准从 PDF 升级为 chunks.json（16 chunk 就在仓库 temp_data，PDF 与 chunk 系统性差异：`#` 标题/`•\t`/空格归一/断行）；13=逐字校验机器化（校验器 `in list`→`in str` 的 bug、最长公共前缀二分定位分叉字符）；14=转换脚本 7 项决策各有线上出处（单源 importlib 加载 ANSWER_PROMPT、neg 合成分必须 ≥0.4 闸门等）。成品：`SFT_Questions.csv` 76 条（pos 47/neg 29）校验全绿 → `sft_alpaca.json` + `dataset_info_sft.json` 待传云端。下一步=第 3 课：开机复核 ③CLI ⑤冒烟 → 裸 0.5B 定性基线。
- **2026-09-02 深夜**：补问题 15–16，**第 3+4 课一晚跑完**。第 3 课：CLI/冒烟复核全绿、数据上传注册（105 个数据集）、裸 0.5B 基线 6 条全败（引用 0/6、拒答 0/3、幻觉 4/6，详见 `baseline_bare.json`）。第 4 课：LoRA SFT 29 秒收敛（loss 1.044→0.264），发现 qwen template 默认 system 的消息结构差异（不重训，理由见问题 16）。新增文档 `微调过程记录分析.md`（项目根）记录微调全程含日志分析。待办：after_sft.py 训后对比。
- **2026-09-03**：**第 4 课收尾 + 第 5 课完成**。after_sft.json 对比到手：训前三败→训后三绿（引用 0/3→3/3、拒答 0/3→3/3、幻觉 4/6→0），暴露三短板（引用标号错位/拒答理由不精确/注水）即 DPO 靶子；adapter_model.safetensors 权重拷回存档，GPU 关机。第 5 课：DPO 偏好对 32 对（用户 8+Claude 24，四配方），validate_dpo.py + convert_to_dpo.py 全绿，28 复用题与 SFT 数据集 instruction 字节相同；补问题 17（四坑：跨题舀水×2、配方形态错位、行尾双空格、目录重组路径断）。下一步=第 6 课：DPO 训练（stage:dpo 从 SFT adapter 续训）+ bare/SFT/SFT+DPO 三方对比。
- **2026-09-03 晚**：**第 6 课完成，0.5B 练手轮收官**。DPO 15.4 秒/12 步训完（accuracies 0.94、margins 2.86——训练指标全绿），但三方生成对比净伤害：pos 引用正确 1/3→0/3、新增假拒 1 例、答非所问 1 例；根因=拒答侧对子占比 17/32 导致过矫正 + 间隔靠打压 rejected 拉开 + 训练指标与生成质量脱钩（问题 18）。**最佳 checkpoint 定格 SFT 版**，DPO 产物留档 sft_output_0.5B/dpo/ 作反面教材。六课全部完成：工具链（造数据→校验→转换→注册→SFT→DPO→生成对比）100% 跑通，四条 DPO 教训带进 7B 正式轮。
- **2026-09-17**：补问题 19（lumy 链路 reranker 精排优化）：78s 疑云定性为算力物理账（排除法四假设全证伪）；三重优化上线——线程 4→8（env 引导收口 knowledge/__init__.py）、max_length 512→256（config `RERANK_MAX_LENGTH`）、模型 large→base A/B 转正（`.env` `BGE_RERANKER_PATH` 开关）。端到端验证 lumy 13/13 零回归、拒答阈值 0.4 无需重校准（Top5 分数分离度健康）、精排节点 50s→5.1~7.2s（~12x）。基准脚本 scripts/bench_reranker_threads.py、scripts/test_bge_reranker_base.py 留档。
